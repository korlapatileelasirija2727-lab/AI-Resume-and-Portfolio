{
  "name": "resumeai-pro",
  "version": "2.0.0",
  "description": "ResumeAI Pro — Python + Node.js agentic resume builder",
  "main": "server.js",
  "scripts": {
    "start":    "node server.js",
    "dev":      "nodemon server.js",
    "backend":  "cd backend && uvicorn main:app --reload --port 8000",
    "setup":    "npm install && pip install -r backend/requirements.txt"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
